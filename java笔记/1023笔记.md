## 1023笔记

### 1.质数

### 2.数组

同一类型的很多变量的集合，数组下标从0开始，关键字是array

int i=3;

int j=4;

int k=5;

想通过一个变量，控制很多值

### 3.作业

常用编程思路：

3.1 控制下标，控制值

3.2 把一个数组看成二份

3.3 与循环结合，遍历

### 4.内存分配：

线程共享：

4.1 堆：用来存储new出来的数据(对象 数组),占到内存的八成，还放常量

Book b1=new Book()

int[] arr=new int[]{111,12}

4.2 方法区：类信息，运行时常量，静态池

静态内容只有一份，

线程私用：

1.虚拟机栈：基本类型变量和引用变量（自己本身没有内容，是有内容的地址值）

### 5.二维数组

一维数组中，有下标和对应的值，

当值为地址值，值展开又是一个数组

### 6.自定义方法

方法，就是做一件事，

概念：有一段具有特点的代码块，叫方法，也叫函数

目的，把这个功能的代码块，封装起来，调用起来方便

public  返回值 方法名(){

  方法体

}

public int add(int a,int b){

return 

}

public void add(){

}

有返回值：做完一件事，会有结果出来，此结果其它地方要使用，有return

比如 id 101的成绩查出来，有返回值

浏览id 101的信息，有返回值

无返回值：做完一件事，不须把结果返给其它地方使用，void

让id 101的信息删除

id 101的信息，库存改为100

参数：

形参：方法为了完成功能，假定的值，只在方法内有效果

实参：函数调用时，真实传入 的值

看要不要参数：看做的事，是个体的事，还是公用的事

公用的事不用参数

delete from user where id=101;

适用场景：

1.有代码块，须要封装的地方，a值也要执行此代码块，b值也要执行此代码块

### 7.使用循环 自定义方法 数组

案例1：使用自定义方法和循环，完成，在控制台输入二个数，能得到最大公约数和最小公倍数

最大公约数：16  24  8

### 8.使用数组完成对学生信息操作

思路：

1.有数组，存这些信息

2.一直操作 while

3.在数组增加信息 arr[i]=xx  arr[0]=1  arr[1]=xx 

​    查询:如果是学生名的数组，String[] arr=new String....

   先把此数组遍历 for(int i=0;i<xx;i++){

​       if("xx".equals(arr[i]))

​            }  

   修改：先找到此元素，arr[i]=yy

编程经验：

当变量操作值有困难，要想到使用第三个变量

数组下标从0开始，连续 0 1 2 3 ...

作业：

程序题：

1.出租车计费里程费

  通过自定义方法  可输入出租车走的路程

  如果3公里以内，收费10元

  如果3公里以外，每公里收费2元，

  得到总费用，

2.在控制台中，可对图书信息，书名，价格 作者，

进行 增加 显示列表  查询 退出，

有余力的同学，接着完成修改和删除

3.选做：已知一个数组，有很多元素，都为整数，通过程序，找到元素出现次数过半的元素

输出来

比如  1 2 1 1 2 3 4 5 1 1 

总体思路：把本数组看成二份，

4.今天录屏和代码，发到群，先把今天不熟练的代码，再写一遍，理解清。

明天讲排序算法，面向对象

